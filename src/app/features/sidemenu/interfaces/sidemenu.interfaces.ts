export interface NavOption {
    id: number
    description: string
    imageUrl: string
    headerImageUrl: string
    color?: string
}

export interface Skill {
    level: number
    maxLevel: number
    iconUrl: string
}