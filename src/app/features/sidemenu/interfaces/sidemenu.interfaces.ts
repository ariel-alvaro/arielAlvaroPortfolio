export interface NavOption {
    id: number
    description: string
    imageUrl: string
    headerImageUrl: string
}

export interface Skill {
    level: number
    maxLevel: number
    iconUrl: string
}